# 独立复核记录

2026-09-25，用户已授权 pi 协助和独立验收。以 main `95ff83a` 的隔离 checkout 为基线，pi 只读复核本轮 `project.mbt`、`main.mbt`、`native_delivery_test.py`，独立执行 29 项交付和 4 项进程监督测试，裁决为通过，未发现具体阻塞问题。

核查重点：非输入文件链接不阻断、目录及输入链接仍拒绝、文档后缀真实目录仍遍历、源码变化失效、部分格式不支持时保留有效 `.mbt` 编译计划并维持 exit 2。

父任务处理与校正：

- 接受跨平台断言建议：文档快照排除断言改为后缀比较，避免 macOS `/var` 与 `/private/var` 导致无效断言；重跑解包产物交付测试。
- pi 文字摘要中的“悬空文档链接忽略”与实际行为不符。父任务用悬空 README.md 实测为 `snapshot_unavailable`；保留此保守边界。
- pi 报告混用了目录扫描与编译计划计数。原始报告确认 native 选择/解析分别为 core 449/449、actrun 56/56；478/62 是普通目录扫描数。
- 部分格式不支持的状态在扫描后不再额外检查快照；它始终为 scope_incomplete / exit 2，不具备 compiler_verified 或生产语义运行资格。本轮不扩大此状态的保证。

语法诊断由另一 pi 独立执行。父任务修正压缩成一行时缺少 case 分隔的 loop 样例，重新运行编译器/扫描器四项最小对照；以 `syntax-diagnosis.json` 和提交的多行样例为准。不将未复跑的版本发布时间、下载可用性或完整迁移可行性作为本轮验收事实。
