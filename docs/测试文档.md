# 测试文档

## 概览

| 指标 | 数据 |
|---|---|
| 测试用例 | 65 个（47 核心规则 + 18 扩展模块） |
| 编译目标 | wasm, wasm-gc, js, native |
| CI 平台 | ubuntu-latest, macos-latest, windows-latest |
| 通过率 | 100% |

## 运行

```bash
moon test                 # 全部测试
moon test --target all    # 全编译目标
moon test --deny-warn     # 警告视为错误
```

## 覆盖矩阵

每条 CWE 规则至少包含正例（应触发）和反例（不应触发）测试。通用规则额外覆盖上下文过滤场景。

| 模块 | 测试数 | 要点 |
|---|---|---|
| CWE-116 replace-escaping | 2 | `replace()` 触发，`replace_all()` 不触发 |
| CWE-79/cmark | 5 | safe=false 触发，safe=true/默认 不触发，含 from_doc |
| CWE-79/inner-html | 2 | 动态参数触发，常量不触发 |
| CWE-79/template | 2 | 字符串插值触发，常量不触发 |
| CWE-94 eval-extern | 3 | eval/new Function 触发，安全 extern 不触发 |
| CWE-113 crlf | 3 | 动态 header 值触发，常量不触发 |
| CWE-942 cors | 3 | credentials+无 origin 触发，显式 origin 不触发 |
| CWE-614 cookie | 2 | 缺安全属性触发，全属性不触发 |
| CWE-770 body-limit | 3 | 有 POST 无 limit 触发，有 limit/无 POST 不触发 |
| CWE-346 ws-origin | 2 | 无 origin 校验触发，有 allowed_origins 不触发 |
| CWE-22 path-concat | 2 | 动态路径拼接触发，常量不触发 |
| CWE-676 unsafe-call | 7 | 危险转换触发；性能操作、guard body 跳过 |
| CWE-248 panic-reachable | 12 | abort 触发；裸 panic、unreachable、guard-else、平台桩、契约断言跳过 |
| CWE-704 unsafe-cast | 4 | cast 触发；extern js、#external 文件跳过 |
| LLM 提示词 | 3 | 生成、解析、JSON 格式化 |
| PoC 生成 | 3 | CORS/Cookie PoC、批量生成 |
| 修复建议 | 3 | 单条修复、去重、报告格式 |
| 基础设施 | 4 | 类型格式化、严重级别、配置、行提取 |

## 测试方法

基于 `@parser.parse_string()` 构造 AST，无需文件系统：

```moonbit
test "CWE-116: replace with HTML metachar should trigger" {
  let source = "fn foo(s : String) -> String { s.replace(old=\"<\", new=\"&lt;\") }"
  let (impls, _) = @parser.parse_string(source, name="test.mbt")
  let findings : Array[Finding] = []
  run_cwe116(impls, "test.mbt", source, findings, Error)
  assert_true(findings.length() > 0)
}
```

## 边界场景

| 场景 | 行为 |
|---|---|
| 路径不存在 | 错误信息 + exit 0 |
| 无 .mbt 文件 | "No issues found" |
| 文件读取失败 | 跳过继续扫描 |
| 无 Web 依赖 | Import 门控跳过 Web 规则 |
| FFI 文件 | CWE-704 跳过 |
| 平台桩文件 (.wasm.mbt) | CWE-248 跳过 |
| guard body 内 | CWE-676 跳过 |
| 标准库模块 | 通用规则全部跳过 |
