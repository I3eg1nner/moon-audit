# 删减前记录

`README-before-redesign.md`、`TODO-before-redesign.md` 和 `cli_regression_before_redesign.py` 保存删减前原文，供恢复后的旧工作区作对照，不能按当前产品说明或当前测试套件运行。

完整源码和原目录布局见项目根目录 `.recovery/2026-09-22-before-redesign`。该本机归档不随 Git 提交上传。旧 CLI 套件使用原路径假设；请在恢复目录中运行原始 `scripts/cli_regression_test.py`。

旧 `tests/cases`、`docs/review-fixtures` 和框架 corpus 脚本仍保留原始预期。它们未被改成当前有限扫描器的“通过用例”。新设计见 [重设计方案](../redesign-2026-09-22.md)。
