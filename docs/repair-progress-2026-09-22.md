# 复核修复进度

目标：关闭 2026-09-22 复核的全部问题，并按 suggest.md 中相关语义完成条件验收。基线 `41f76dc`。

## 最新状态

**尚未整体完成。** MoonBit v0.10.14 迁移、原始八项反例修复以及联合状态生产接线已推进。详见 [升级复核](upgrade-review-2026-09-22.md)、[机器记录](metrics/upgrade-review-2026-09-22.json)和 [suggest.md](../suggest.md)。

## 当前工作方向：统一核心的小范围验证

已暂停现有 AST 扫描器的能力扩张。新增独立 IR 可执行规格：15 个语义见证、256 个生成的顺序程序对照及 14 项测试通过；基本块工作队列消除了串联调用的整函数重复计算。**当前未接 MoonBit 前端，状态快照成本与真实语料门禁未通过，生产迁移未批准。**

下一步按 [架构验证](core-semantics-validation-2026-09-22.md) 的 B1 状态成本、B2 真实前端、B3 真实语料顺序推进。下列清单仅为旧流水线的历史工作记录，不再驱动外围能力扩张。

## 当前验收清单

- [x] 新工具链依赖、显式黑盒导入和公开 trait 方法迁移；全目标检查通过。
- [x] 原始八项合法反例在 AST/CFG 模式全部符合预期。
- [x] 默认注册表传递一份联合状态，调用图和污点目标消费已接通；工厂方法断线及热缓存测试通过。
- [x] 七项联合求解测试通过；未收敛信息传给调用图，不完整接收者保留候选。
- [x] CLI 61/61、harness 33/33、cases 通过；显式回调模型回归已修复。
- [x] 格式和生成接口已同步；冻结前最终源码四目标各 437/437；不作为新核心验收证据。
- [x] 固定快照已用最新扫描器重扫，冷/热告警与错误一致，内容哈希已记录。
- [x] 包别名改用官方 AST；单行/多行与显式别名替换语义通过，跨包同名异常不再串用。
- [x] async 字节串 return 解析修复：170 文件解析错误为 0，冷/热扫描及新编译器检查通过。
- [x] 统一展开字节/字符串插值，保留词法绑定和内部调用；简单插值通过独立 CFG 严格验证。
- [x] 解析策略版本更新，并用升级前后真实扫描器验证三个已有缓存条目失效及新热缓存稳定。
- [x] UTF-8 和 core Show 的 20 项合法转换正反例通过；跨包同名类型、辅助 Logger 写入、嵌套格式化、泛型 sink 和热缓存回归通过。
- [x] remaining_probe 的默认依赖/效果、字面量别名三项关闭；默认专项 22 项通过，跨包和热缓存验证通过。
- [x] 默认求值不再污染声明阶段；Option 存在性、抛错时堆状态、模型默认值和递归预算披露有回归。
- [x] 独立污点策略版本进入缓存；三个旧缓存指纹失效且新热缓存稳定。
- [x] remaining_probe 三类字段漏报已关闭；七项中六项通过，普通泛型 trait 仍失败。
- [x] 字段事实区分正常/异常出口，覆盖别名、同名遮蔽、clean、多来源、getter、交换和跨包缓存；23 项合法字段项目中 21 项通过。
- [ ] 多形参绑定同一对象时的读后写依赖漏报和后写清理误报；完整字段探针仍退出 1。
- [ ] 独立 CFG 的字段关系、默认表达式执行尚未实现。
- [ ] 真实语料最终语义验收：mocket/crescent 全模块旧 API/语法编译错误已记录，需固定兼容版本或明确迁移差异。
- [x] 原始 `errdefer` 三项失败已关闭；24 项合法异常场景、跨包身份和两种摘要缓存失效测试通过。
- [ ] 完整循环退出/清理、外部函数错误效果及独立 CFG 异常边仍待验收。
- [ ] 包/类型/trait 身份贯穿旧类型回退、SCC 和传递 sink。
- [ ] 污点与指针共享完整堆对象及字段事实，覆盖跨函数别名。
- [x] 默认参数依赖与副作用 22 项、复合来源字段写入已通过已列合法边界验收；不代表全部别名/控制流语义完成。
- [ ] 真正的 CallSite(1) 上下文策略及固定真实语料语义验收。

历史失败基线保留在 [原始复核](review-continued-2026-09-22.md)；其中“八项反例有七项失败”不再代表当前代码。

旧路线的后续能力扩张已冻结。当前先验证统一核心；完整目标仍未完成，不能凭旧测试通过或 IR 原型通过标记完成。
