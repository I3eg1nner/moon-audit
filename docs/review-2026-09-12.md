# 2026-09-12 项目检查与优化记录

本轮使用三个 subagent 分工，并交叉复审。原定 UTC 15:00 截止，实际收尾至 15:00:44（超时 44 秒）；当时改动保留在工作区，未提交或推送。

后续补跑与修复见 [2026-09-13 记录](review-2026-09-13.md)。下文保留当时的验收状态。

## 已完成

- 污点正确性：修复增量赋值丢失左值污点、Guard 失败分支漏检和常量死分支误报；分支对象别名改为候选集合合流，保留单对象强更新与多对象弱更新；补齐 Record/RecordUpdate 的 CFG 遍历。
- 缓存：键纳入实际声明源码、位置、规则、severity、Output 开关及过程间摘要；符号指纹覆盖语义字段；隔离可变结果数组。缓存仍为进程内缓存。
- CLI：精确项目相对路径与空增量列表，所选文件范围内的解析诊断；多 --rule 启用与累积、独立 Output 规则及 deep 模式开关；无效输入和失败写出返回 2；机器格式的诊断保持结构化。
- baseline：JSON 序列化支持 Unicode/控制字符；新 baseline 使用项目相对路径及文件级身份；报告比较按规则、文件、指纹和出现次数匹配，并保留 analysis_scope。
- 验证基础设施：脚本正确传播编译/扫描/超时失败，隔离 C8 默认参数调用断言，清理临时目录；Action 输入通过环境变量传入，结构化计数并阻止发布无效或旧报告。
- 扫描开销：普通 CLI 采用库入口已有的 with_symbols=false，跳过 TaintFlow 未消费的依赖符号重建；尚未完成严格性能基准，不宣称加速倍数。

## 验证

- 原始基线：327/327 × wasm、wasm-gc、JS、native。
- 最终版本：372/372 × 四目标，均使用 --deny-warn；45 个新增 MoonBit 测试。
- 最终 CLI 端到端：12/12；脚本/Action 离线自测：26/26；真实 tests/cases/run.sh 门禁通过。
- 最终 moon fmt、moon info、四目标 moon check --deny-warn、native build 及 git diff --check 通过。
- 本地 Linux 验证完成；三平台 CI 配置已更新，尚未推送或在远端执行。

## 语料对照与未完成项

- 已有原始版本基线 19 个项目；截止前完成 16 个告警逐条对照，均一致。
- 10 秒单项目预算超时：mizchi_crater, moonbitlang_moonyacc, moonbitlang_openseek。
- 截止时仍未收口：上述三个项目，以及全量类型/调用图和正式性能对照。
- actrun 的 9 个解析错误原始版本已经存在；新 CLI 正确返回 2，不再把不完整扫描视为成功。
- 原始调用图/类型统计已保存；最终全语料调用图和严谨性能对照未完成，不作为本轮已通过项。
- 既有能力边界：完整 return/raise/defer 退出语义与 Guard 模式绑定、跨进程持久化缓存、上下文敏感分析仍需后续推进。
- baseline 中无 file 的旧条目继续全局匹配；过滤型 baseline 仍按文件内指纹匹配，报告比较另支持出现次数。

## 结果文件

- 原始测试：`/tmp/moon-audit-baseline-tests.log`
- 最终检查：`/tmp/moon-audit-close-{fmt,info,check,build,tests,cli,harness,cases}.log`
- 原始语料：`/tmp/moon-audit-original-corpus/`
- 最终扫描与对照：`/tmp/moon-audit-final-corpus/`
