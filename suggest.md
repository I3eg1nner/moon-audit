# moon-audit 修复推进建议

整理日期：2026-09-16。

本文整理前一轮关于“为什么一直没修完善成功”的分析和执行建议。事实依据是对提交 `5b2d867` 的[差距复核](docs/review-taie-gap-2026-09-16.md)与[验证记录](docs/metrics/taie-review-2026-09-16.json)。工作区后续修复需要重新验收，本文不把旧结论直接当作最新代码的状态。

## 一、为什么修复总是没有闭环

主要原因是：**实现与验收经常停在局部结构上，没有证明真实入口使用了这些结构，也没有证明结果符合预期语义。** 单个反例修复后，底层身份、事实传播和入口接线仍不统一，于是相邻场景继续出现同类问题。

| 原因 | 已验证的例子 | 应改变的做法 |
| --- | --- | --- |
| 局部补丁没有覆盖共同根因 | 加入 `Type::method` 修好了类型同名反例，跨包同名仍串用摘要 | 从定义、调用绑定到摘要消费统一函数身份 |
| 测试断言不能区分正确与错误 | 告警总数都是 2，但可能是正确两处，也可能包含一处漏报和一处误报 | 同时断言必须出现和必须消失的具体告警 |
| 直接 API 正确，生产适配层断线 | 直接调用图 API 有事件，经默认注册表为 0；库入口与 CLI 的符号加载选项不同 | 经过真实 CLI 和默认注册表验收 |
| 新机制仍依赖旧机制的结果 | CFG 执行前由 AST 生成事实，预计算表遮蔽按当前块状态求值 | 用接口约束建立可独立执行的最小语义链 |
| 配置与能力声明先于求解能力 | CallSite(1) 标记为已实现，普通调用却没有形成上下文摘要 | 检查实际节点身份、传播事实与可区分结果 |
| 指标与名称含义不一致 | 指向集合非空计为目标集合缩小；缓存命中使执行诊断下降 | 明确定义每个指标的分母、触发条件和缓存口径 |
| 一轮同时推进过多基础能力 | 身份、CFG、上下文、指针桥接、事件、缓存交叉变化 | 每轮只关闭一个语义链，完成后再扩大范围 |

这不意味着现有工作没有价值。共享依赖结果接口和原类型同名反例都有实际进展。问题在于，局部进展被过早解释成了整个能力完成。

## 二、先改变修复与验收方式

### 1. 每项修复先提交一个会失败的语义测试

测试至少说明以下内容：

- 输入程序应能通过编译，避免把无效程序行为当作分析缺陷。
- 必须报告哪个 sink，必须不报告哪个 sink。
- 测试经过哪个入口：CLI、默认注册表或底层求解器。
- 当前版本为什么失败，修复后预期发生什么变化。

对接线类修复做一次临时断线验证：撤掉新增的结果传递或事件转发后，对应测试必须失败。无需引入完整变异测试框架，只需确认测试确实覆盖了这条连接。

不要用以下断言证明功能完成：

- 集合长度大于等于 0。
- 只检查不崩溃、目录存在或 `implemented=true`。
- 只检查告警总数，不检查告警位置与类型。
- 用直接 API 测试代替生产入口测试。

### 2. 将完成状态拆成四级

| 状态 | 所需证据 |
| --- | --- |
| 接口已存在 | 数据结构和调用接口可用 |
| 生产路径已接入 | CLI、默认注册表和实际消费者确实使用它 |
| 语义已验证 | 正反例、边界条件和不变性测试通过 |
| 真实语料已验证 | 固定版本语料有可复现结果，关键发现经过核对 |

提交说明和能力文档应写清达到哪一级。对尚未支持的上下文策略或语义，应明确报告支持范围，避免仅修改测试预期来消除失败。

### 3. 每轮保留一份可复核的验收记录

记录代码版本、工具链、语料版本、运行命令、缓存状态、预期与实际结果。区分告警精度、解析覆盖、调用绑定覆盖和执行耗时，不用一个汇总数字替代其他指标。

## 三、下一轮只完成全局函数身份

**下一轮目标：让 ProgramWorld、调用绑定、SCC、摘要生成和摘要消费使用同一套函数身份，关闭跨包/trait 同名污染。**

### 改动顺序

1. 在 `ProgramWorld` 中建立规范的 `PackageId`，由模块与包信息确定归属。
2. 建立结构化的 `FunctionId`：至少区分所属包、普通函数/固有方法/trait 实现，以及所属类型和 trait；具体字段以 MoonBit 的合法定义规则为准。
3. 建立 `CallSiteId`：包含调用者身份和表达式位置或节点编号，能区分同一行中的两个调用。
4. 在绑定阶段解析本地调用、包别名调用、方法调用和 trait 调用；静态调用得到目标 `FunctionId`，动态调用保留候选集与解析状态。
5. SCC 从绑定后的调用关系建边，摘要表用 `FunctionId` 存取，污点传播消费同一绑定结果。
6. 检查调用图、指针节点和缓存键中的函数身份，移除会重新合并不同定义的短名称键。

展示给人的名称可以单独保留，分析内部身份不能依赖展示字符串的拼接或后缀猜测。

### 应避免的补丁

- 从第一份源码文件所在目录推断整个项目根目录。
- 把导入函数一律加上调用者包前缀。
- 在摘要表中搜索唯一名称后缀，并将“暂时只有一个候选”当作绑定成功。
- SCC 用一种名字，摘要生成用另一种名字，消费时再尝试兼容。

这些做法会让新增无关包、改变文件遍历顺序或引入同名符号影响已有程序的分析结果。未解析调用应显式记录并按既定保守策略处理，不能静默借用另一个函数的摘要。

### 必须通过的验收

| 用例 | 预期 |
| --- | --- |
| 不同类型的同名方法 | 各自命中正确摘要，保留已有修复 |
| 不同包的同名函数 | 脏数据返回路径报告，安全返回路径无告警 |
| 合法的不同 trait 同名方法 | 按实际实现区分身份与目标 |
| 包导入与别名调用 | 绑定到被导入包中的定义 |
| 文件遍历或声明顺序变化 | 规范化后的语义结果一致 |
| 无关符号重命名或新增无关包 | 原有调用绑定与告警不变 |

测试应同时检查绑定目标和最终告警。重排导致源码行号变化时，按具名函数/sink 标记比较语义结果；对固定反例仍应精确断言告警位置。增加递归及互递归用例，确认 SCC 改造后仍可收敛。

**完成条件：**上述测试通过，真实 CLI 能复现修复效果，已有相关回归通过，正常解析路径不再依赖短名称或后缀查找摘要。

## 四、随后建立真正独立的最小 CFG 执行链

先限定受支持的子语言：常量、变量复制与赋值、条件分支、普通调用、返回、source/sink。闭包、异常、defer 和复杂堆语义分别扩展验收。

### 具体实现要求

- IR 必须携带可执行的语义，例如 `Const`、`Copy`、带实参与返回目标的 `Call`、`Branch`、`Return`；需要表达式操作时补充对应指令。
- CFG 执行器只接收 IR、模型、被调函数摘要及分析配置，不接收 AST walker 生成的污点结果表。
- 表达式使用当前块状态求值，分支状态通过显式合并规则汇合，循环按工作队列迭代到稳定状态。
- AST 可作为对照实现，不得成为新执行器语义正确性的隐含输入。
- 遇到尚不支持的语义时，明确按整个函数回退，并记录原因；避免在一次分析中混用两个执行器的中间事实。

### 验收用例

1. 常量赋给局部变量再传入 sink：无告警。
2. source 赋给局部变量再传入 sink：有告警。
3. 污点变量被安全常量覆盖：按覆盖后的状态判断。
4. 只有一个分支含污点：合并后保留可能的污点。
5. 普通调用返回污点：通过正确摘要传播。
6. 循环需要多轮传播：结果稳定，不依赖遍历顺序。

**完成条件：**受支持子集无需预先执行 AST 污点分析即可独立输出正确告警；相应反例的冷扫描与 `--cfg-verify` 均通过。不支持的函数具有可见回退原因。

## 五、统一指针与调用图后，再做真正的上下文敏感

建议依赖顺序：

```text
统一身份与调用绑定
  → 可独立执行的基础 IR
  → 上下文不敏感的指针/调用图联合求解
  → 污点分析消费统一事实
  → 真正的上下文敏感
  → 插件扩展与性能优化
```

### 先完成上下文不敏感的联合求解

打通以下循环，直到没有新增事实：

1. 新指向事实触发动态调用目标解析。
2. 新调用边加入实参到形参、返回值到调用结果的传播约束。
3. 新约束产生更多指向事实，重新触发相关调用点。

堆对象与字段传播必须保留统一的抽象身份，避免跨过程传递时降成仅按类型归并的占位字符串。污点分析可保留现有流敏感能力，但应消费同一调用目标和堆事实。

默认注册表需要明确依赖关系并传递结果。事件应覆盖其名称承诺的新增事实；接入后，以真实入口测试验证回调和反馈均可继续驱动求解。

### 再验收 CallSite(1)

上下文必须进入实际的方法实例、局部变量和所选堆抽象的身份与传播过程。堆上下文策略应单独说明，不能只给摘要键增加字符串。

验收需要一个能区分上下文策略的程序：同一个分配点经两个不同调用点产生对象，一条路径写入污点，另一条保持安全，检查对象与字段事实是否按设计隔离。同时检查上下文节点、调用目标和最终告警。

`helper(x) { x }` 分别接收安全参数和污点参数并不足以证明上下文敏感：普通参数到返回值的关系摘要也可以得到正确结果。同一行的两个调用也必须具有不同 `CallSiteId`。

## 六、缓存、指标与入口的收尾标准

这些问题需要各自的回归测试，可作为独立修复提交；它们不应被用作核心分析能力已经完成的证明。

| 项目 | 最小验收 |
| --- | --- |
| CLI 与库入口一致性 | 必需的符号信息、配置和分析依赖一致；真实 CLI 能触发新增能力 |
| 注册表事件 | 在应有事件的程序上精确断言事件内容；断开转发后测试失败 |
| 指针精度指标 | 分开统计有指向信息、严格缩小、等集、空交集回退；人工反例有明确预期 |
| 缓存生命周期 | 独立进程首次写入、再次命中、源码变化后更新、依赖变化后失效 |
| 缓存诊断 | 分开显示已执行、缓存复用和未支持数量；缓存命中不被误解为分歧消失 |
| 真实语料 | 固定代码版本，区分解析失败与零告警，人工核对关键发现 |
| 性能 | 记录 release 构建、冷/热缓存、相同语料与配置；先保证结果可比 |

## 七、下一项任务可以直接这样下达

> 完成全局函数身份闭环。先把已复现的跨包同名漏报/误报写成真实 CLI 回归测试，精确断言安全与危险 sink；再由 ProgramWorld 提供包身份，在绑定阶段产生统一 FunctionId，让 SCC、摘要生成和摘要消费共用它。补齐类型/trait 同名、导入别名、顺序变化、无关符号重命名与递归测试。本轮范围限于身份链路及其必要适配。交付修复前后的测试证据、受影响模块说明和剩余限制。

后续每一轮都采用同样的完成标准：**一个明确的语义问题，一条真实入口链路，一组能证明修复有效的验收测试。**
