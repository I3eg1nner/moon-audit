# moon-audit 当前建议与复核记录

当前状态以本节、[TODO](TODO.md)和[架构方案](docs/architecture-plan-2026-09-24.md)为准。下方折叠内容是阶段历史，不再作为当前能力或下一任务清单。

## 合并后的实测意见：先补可用性与证据

热门项目实测支持继续保持有限定位：24 个固定样本中 23 个成功导入，8 个扫描不完整；15 个仅完成声明的语法范围。发现了历史语法缺口、文档负例混入和无关文档链接阻断快照等具体问题。下一阶段应优先解决这些可复现问题，而不是增加未核实规则。[完整检查与优先级](experiments/popular_projects/README.md)

自定义 OpenAI 兼容 API 已实现并用 `.env` 真实联调。两条 BioSeqs 线索的模型意见保留为 needs_review；人工用固定 core 复现了重复 XML 字符只替换首处。这个结果说明复核层需要真实绑定和库语义附件，而不是更强的结论措辞。LLM 意见不升级静态证据；函数级复现也不等于完整应用可利用。

## 本轮优化后的判断

core/actrun 的文档文件链接已不再阻断验证；native 编译计划分别选取并成功解析 449、56 个 `.mbt`，仍因 66、1 个 `.mbt.md` 保持不完整。35 个不属于 native 计划的文件不再混入验证扫描；告警数量未改变，不能把范围改正宣传为安全准确率提升。[复测与验收](experiments/popular_projects/optimization-2026-09-25/README.md)

前端是下一阶段更明确的瓶颈：`try?`/旧 `loop` 被固定编译器接受，却被 parser 0.4.0 拒绝。建议先补兼容层的语法表示、原始位置和错误契约，评估官方历史产生式的最小回移或独立前端；不要直接引入整套旧引擎或用文本替换伪造支持。LLM 上下文增强仍需真实绑定与库行为证据，不能弥补未解析文件。

## 2026-09-25：有限安全检测已形成生产闭环

项目定位已经落实为面向源码自编译/二进制下载用户的本地 MoonBit 安全检测工具。A–D 的有限功能已实现并通过 Linux 本地验收：稳定的语法扫描、项目版本与文件范围验证、少量真实模型和显式限定范围的可选语义模式。**三平台交付验收已通过**：Windows 路径身份与 macOS 内存监督修复后，Linux/macOS/Windows 的真实 CI 全部成功。[平台证据](docs/metrics/three-platform-acceptance-2026-09-25.json)保存提交号和归档指纹；PR #1 已合并到 main（`075b0f3`），尚未正式发布。

### 现在有证据支持的判断

1. **扫描器可以保留，但应减少默认噪声。** 14 条规则已逐条裁决，只有 replace-escaping、cmark-unsafe 默认启用，全部语法发现仍为 `syntax_hint`。44 项编译形状和98项登记/选择/门控/严重度一致性验收通过，不等于安全准确率。真实 mocket/cmark API 有编译依据；Crescent 旧 lexscan 失败保持未核实。独立复核还修复了 CWE-942 忽略用户严重度的问题。[规则审计](experiments/rule_audit/README.md)
2. **独立核心值得保留，已经复用于第二库。** mocket 查询经普通 String helper 到最终返回 HTML 的链由真实源码进入官方绑定和顺序 IR。cmark 渲染复用同一表示与求解规则；没有把同名调用或旧 AST 告警冒充数据流。[cmark 生产 14 项验收](experiments/cmark_chain/production-ir-2026-09-25.json)
3. **生产接入已经完成受限验收。** `--analysis semantic --verify-project --semantic-scope mocket-get-callbacks` 在 native 下启用；15 项 Linux 生产入口验收涵盖冷/热一致、报告、baseline、未知范围和资源故障。语义 worker 60 秒、256查询/4并发、每回调10000 IR单位，以及 Linux 每进程2048 MiB `RLIMIT_AS` 地址空间硬上限已有实际失败对照；这些结果不替代其他平台验收。[生产验收](docs/metrics/semantic-production-acceptance-2026-09-25.json)
4. **历史版本接入和语法兼容必须分开评价。** 2025 QuickCheck 原始源码在对应旧工具链下可检查，30文件选源一致；当前parser仅解析16个、14个失败并exit2。它证明可以接入真实老项目，也证明不能承诺完整分析任意老版本。[历史验收](experiments/historical_project/README.md)

### 对当前边界的意见

可选语义不是通用 MoonBit 安全分析器。它不证明注册代码可达、中间件是否改变响应、未知路由注册形式、通用堆别名、分支或异常。`verified_dataflow` 只证明声明模型与回调前提下的路径，不证明浏览器可利用性。

cmark 的 `safe=true` 结果是限定正文位置的 HTML 片段，不是通用文本编码。`safe=false`、编码后再 unsafe 渲染、safe片段放进script位置等保留 `partial_dataflow` 并exit2；不能为追求“全部绿色”把这些边界改成安全结果。

内存预算必须按平台读：Linux 是每进程2048 MiB `RLIMIT_AS` 地址空间硬上限，Windows 是每进程2048 MiB提交内存硬上限；两者都不是整棵进程树的RSS总预算。macOS 改为每50 ms采样进程组 physical footprint 并求和，超过2048 MiB或无法采样就终止整个组，允许短暂超额，不能称硬地址空间上限。macOS 真实超额分配已触发组终止且无存活子孙；Windows 正常语义与路径身份验收通过，但未专门注入提交内存超额。报告 `memory.mechanism/scope/hard_limit` 和 `ir_units_per_callback` 应清楚反映差异。60秒不是整个CLI的总时限；固定语料冷/热测量也不能外推到任意规模项目。

### 接下来如何推进

- **进入审查交付。** 三平台真实 runner、解包搬迁和归档指纹已核验；既有成果已合并 main；后续改动仍需审查和验收后再考虑发行。后续提交继续运行相同门禁。
- **再按真实样本补语言适配。** 优先2025历史语法和已知新`for...in`缺口，验证AST/位置/规则结果；未支持时维持不完整，避免按版本号笼统放行。
- **新语义只跟随明确的用户检测场景。** 新库先验证运行效果、身份、依赖和版本，再增加模型；字段/异常/trait等只有真实链确需时逐项验收，不重新建立两份堆和名称回退逻辑。

因此，建议维持“有限扫描器 + 已验证小核心”的定位。当前阶段有可交付成果；未来价值来自提高已声明范围的可靠性和实际可用性，而不是继续追逐Tai-e的完整功能表。

<details>
<summary>历史复核与路线演变（均为当时状态，后续执行以本文上方为准）</summary>

## 2026-09-24：先验证真实 API 模型，再决定是否扩展分析语义

**新复核改变了下一步顺序。** 当前独立实验已经证明“无堆、直接调用、`String` 参数/返回”可传播，但固定 Web 库快照里的实际请求和响应 API 并不完全落在这个子集内。继续把实验函数 `source()`/`sink()` 换个名字，不能算真实安全检测。

- 本地 mocket `0.9.1` 快照 `af354b7a` 的 JS 与 native 响应输出路径都会调用 `sanitize_header_value` 清除 CR/LF；它应先作为**安全负例**，不能仅凭 `set_header` 名称列为 CWE-113 危险 sink。证据位于 `/data/my/corpus/oboard_mocket/mocket.js.mbt`、`mocket.native.mbt` 和 `internal/header/header.mbt`。
- 本地 Crescent `0.11.0` 快照 `5836fe84` 有 `HttpRequest::path() -> String`、`get_header() -> String?` 和 `HttpResponse::header(String, String) -> HttpResponse`。`header` 把值写入响应 headers，后续经 `@http.ServerConnection.send_response`；传输层是否拒绝/净化 CR/LF 尚未验证。现有 CWE-113 语法规则只匹配 `set_header` 等名称，**未匹配 `header`**；现有无堆适配器也不支持这些对象方法及可选值。证据位于 `/data/my/corpus/moonbit-community_crescent/core/{request,http_headers,response}.mbt` 和 `serve_response.mbt`。不能据此宣布 Crescent 存在可利用的响应拆分漏洞。
- 对已创建的 `/tmp/moon-audit-upgrade-20260922/compiled-corpus-crescent` 副本执行 `moon check --target native core`：目标 moon `0.1.20260904`、`0.1.20260915`、`0.1.20260920` 均因依赖包 `cookie.mbt` 的 `lexscan` 对 `String`/`StringView` 类型报 `E4222` 而失败。这是**固定旧语料与所测编译器不匹配**，不是分析器解析或安全规则的结论；当前 Crescent 候选连“真实编译”门槛都未通过。应先找与该快照匹配的旧编译器或固定一个可编译的新库快照，记录库、依赖和编译器版本后再继续。

### 下一轮采用四个门槛

1. **先通过候选可编译性门槛。** 当前 Crescent `0.11.0` 候选在三套已测目标工具链下均未通过。先选一个与库及其依赖匹配的项目工具链或另一个可编译的固定库快照，构造最小真实调用项目；再用官方绑定查询核实 source/sink 的包与声明身份，并实测 CR/LF 到实际 HTTP 输出时是否保留。若任一门槛无法稳定复现，就停止把 CWE-113 作为本轮垂直目标。
2. **只在门槛通过时增加最小 API 适配。** 请求/响应对象先作为不展开字段的**不透明句柄**；只支持已绑定的 `path()` 返回 `String` 与 `header(name, value)` 的 value 参数，字符串经普通直接 helper 传播。暂不实现通用堆、方法派发或 `String?`；用到这些能力时明确返回不完整。仍以独立实验输出，不迁移生产 CLI。
3. **正反例必须判别模型是否真实。** 同包真实方法阳性、安全常量、同名无关方法、mocket 已净化输出、对响应值做有效清理的样例都要有精确预期；若无法说明实际输出效果，只报告“待人工核查的数据流”，不标 CWE-113 已证实漏洞。
4. **同时增强有限扫描器。** 按规则检查调用形式、真实 API 身份及版本语法差异，先补按文件状态清单和 per-rule 已验证/未知范围。这个产品路线独立于跨函数实验，可继续交付。若需要不断为第一条真实链加例子专用 AST 分支，则停在可信有限扫描器，不再扩大求解器。

这一步是**API 可行性裁决**，不是扩大到 Tai-e 的对象分析。下节先前写的“直接实现真实无堆 CWE-113 链”以本节门槛为准。

---

## 2026-09-24：有限扫描器交付与无堆 String 流实验

**路线收缩：不以复现 Tai-e 的能力范围为目标。** 生产入口保持有限规则扫描器；跨函数能力只在独立实验中逐项验证，未通过真实源码与明确拒绝边界前不迁移。近期验收单位是 `规则 × 调用形状 × 目标工具链 × 前端能力`，不是测试总数或单个版本号。

### 本轮已经落地

- CWE-116 已通过共用的[语法调用视图](src/call_view.mbt)覆盖 `s.replace(...)` 与 `String::replace(s, ...)`。视图只规范调用形状、显式类型名、参数和位置，**不声称已取得真实绑定或推断点调用接收者类型**。显式 `String::replace_all`、非危险替换、其他类型同名方法有阴性测试。三套目标 moon `0.1.20260904` / `0.1.20260915` / `0.1.20260920` 均通过真实编译、运行等价性与正式扫描器正反对照；此前静默漏报已关闭。[更新后的矩阵](docs/metrics/project-version-compat-2026-09-24.json)仍标 `verified_limited`，因为 `for (x, y) in ...` 在这些编译器下合法而 parser `0.4.0` 拒绝。
- 扫描结果新增选中文件数与成功解析且执行规则的文件数，保留旧 `files_scanned` 字段。解析失败和默认排除项在报告范围中可区分；仍未提供每条规则或每个文件的语义覆盖证明。
- [隔离的前端适配实验](experiments/frontend_adapter/README.md)新增不含堆对象的直接调用 `String` 参数/返回子集，复用现有 Python 核心与官方 `peek-def` 绑定核实。真实源码的 `source() → relay() → sink()` 阳性及安全字面量/固定安全返回阴性在上述三套项目编译器下均通过；七组源码对照均与[机器基线](docs/metrics/frontend-adapter-2026-09-24.json)一致。`source`/`sink` 仍是实验模型，不是实际 CWE-113 API。Box 字段加 String 返回的组合明确拒绝，不把两个已验证子集合并为已验证能力。

### 下一步只做三个可验收的小交付

1. **补齐覆盖事实。** 本轮 JSON/SARIF 已新增 `files_selected` 与 `files_parsed`；旧 `files_scanned` 保留“已读取并尝试解析”的兼容含义，解析失败不再误作成功解析。`analysis_scope` 同时披露计数及默认排除项。下一步增加按文件的选中/跳过/失败清单，并区分“语法通过”与“该规则调用形式已验证”；目前仍不能用零告警证明覆盖完整。
2. **验证一条真实无堆安全链。** 选定有可复现依赖版本的真实输入与响应头 API，按官方定义身份绑定 source/sink；只允许单文件、显式 `String`、直接调用、参数/返回传播。安全常量、清理函数、同名无关 API 和重命名对照必须不告警。字段、动态派发、异常、递归及跨包先返回 `unsupported`/`incomplete`，不得猜安全或借用旧 AST 结果。只有真实 CLI 或清晰标记的独立实验输出通过正反例，才宣称该子集成立。
3. **再扩大语法和范围。** 修 `for...in` 前端缺口并保持解析失败的不完整状态；新增旧版 MoonBit 目标时先编译真实语料，再验解析、规则调用形状和绑定。若无堆直接调用仍需要大量案例特判，就停止扩建通用求解器，继续改进有限扫描器。堆、别名、递归与性能优化暂不进入本阶段目标。

**发布门槛：**有限扫描器可独立交付，文档必须披露规则和版本语法覆盖；跨函数实验不得被计入生产扫描能力。大幅删减仍在未提交工作区，发布前应固定可复核的代码版本和矩阵证据。

---

## 2026-09-23：目标是检测使用不同 MoonBit 版本的项目

**更正上轮判断：**固定 MoonBit 版本只适合复现分析器构建和实验结果，不能作为目标项目的版本限制。`moon.mod` 的 `version` 是模块版本，不是编译器版本。分析器和待检测项目应分别选择工具链；项目侧版本以实际 `moon version --all`、真实编译和源码解析结果为准，不以补丁版本白名单放行。

已新增[严格跨版本扫描入口](scripts/scan_compatible.py)和[兼容性说明](experiments/version_compat/README.md)。它用项目指定的工具链 `moon check`，再用现有扫描器扫描；编译失败、解析失败和扫描失败各有明确状态，解析失败时保留其他文件已发现的问题。三套目标 moon `0.1.20260904` / `0.1.20260915` / `0.1.20260920` 均通过便携语法的正式 CLI CWE-116 正负对照；独立核心的五组源码对照在三套目标编译器下也一致。[矩阵证据](docs/metrics/project-version-compat-2026-09-23.json)已记录，CI 对三个目标版本逐一验收。

**关键缺口：**上述三个编译器都接受 `for (x, y) in ...`，随工具打包的 parser `0.4.0` 却拒绝它。严格入口会报告 `scanner_incomplete`；这不是“该版本不支持”，而是分析器语法前端落后于目标编译器。已实测官方 `untyped_cst` 回退同样报错，因此撤回未通过的接入；须等可用官方解析路径或单独验证的兼容适配后，才能扩大支持声明。正式扫描器目前只用 14 条规则中的 1 条完成跨版本真编译验证，不能宣称全规则或任意版本兼容。

后续每次官方语法变化的流程：以目标工具链编译真实正负语料 → 检查分析器每个文件能否完整解析 → 对比规则发现与独立核心结果 → 若出现缺口，按语法能力更新官方 parser 或增加隔离的前端适配，并维持明确的不完整状态；不为每个补丁版本复制整套求解器，也不在解析失败的文件上用 AST 猜测安全。本段是 2026-09-23 的阶段判断；显式调用漏报已按上方 2026-09-24 记录关闭，剩余解析缺口和覆盖披露继续按新顺序处理。

---

## 2026-09-23：受限前端已接入独立核心，建立语法升级门禁

用户确认：**保留有限扫描器，独立重建核心**。本轮不再删减生产扫描器，也不恢复旧框架。

已用三包合法项目验证官方 `peek-def / hover / find-references / gen-symbols`。14 项查询中 13 项符合预期，泛型方法导航返回约束位置，保留该不满足契约项；回调导航只返回形参。批量索引有 20 条记录，但未提供顺序 IR、参数绑定和异常边。

**受限 G0 子集已经贯通，但完整 G0/G1 尚未通过。** 五组编译通过的真实 MoonBit 源码已由官方 AST 加 `peek-def` 自动降为独立核心 IR，分别验证同对象别名、覆盖顺序、不同对象及局部遮蔽。实验的 `source`/`sink` 是明确配置的测试模型，不是 CWE-113 真实 API。当时返回、异常、递归、跨包和真实安全链仍未接入；2026-09-24 已在独立无堆子集中加入 String 返回，其余边界仍未通过，不能迁移生产入口。

证据、可复现命令及事实来源契约见 [G0 入口复核](docs/frontend-contract-2026-09-23.md) 和 [受限适配实验](experiments/frontend_adapter/README.md)。

官方 parser 明确标注实验性且不稳定。分析器构建侧保留固定 moon/moonc/parser 版本，目标项目侧运行跨版本矩阵；原 CI 最新版本通道继续显示上游变化。升级时先复跑真实编译、完整解析和源码对照，再审查结构与绑定差异，确认后更新适配代码与机器基线。当前机器记录见 [前端适配结果](docs/metrics/frontend-adapter-2026-09-23.json)。

**独立核心后续项：**在不扩大语法范围的前提下，把真实返回值和异常出口接入同一堆语义；补递归源码对照和真实 CWE-113 API 绑定模型。此项在上方 2026-09-24 的前端覆盖契约之后推进。只有完整安全链、拒绝边界和资源预算共同验收后，才考虑迁移入口。

---

## 已执行：选择性删减，建立有限扫描基线

用户已允许大幅删减或回退。本轮保留当前解析/规则/报告等基础代码，参考跨函数扩张前的边界移除旧框架；**Git HEAD 和 index 未重置**。活动 MoonBit 源码（含测试）从 37,401 行减至 4,330 行，96 个文件减至 28 个。

当前交付是 `0.5.0-dev` 语法模式扫描器：保留 14 条模式规则和基础 CLI，明确不提供跨函数、类型或净化解析。旧框架和 LLM 等入口已移除；CWE-113 只保留低置信度动态头部提示。源码完整恢复点位于 `.recovery/2026-09-22-before-redesign`，不要删除。

四后端各 66 项测试、当前 15 项 CLI 回归在工作区通过。旧语义反例保持原预期，不能把减少后的绿色测试解释为原能力全部修好。具体删除清单、行为变化、恢复方式和新核心四层设计见 [精简与重设计方案](docs/redesign-2026-09-22.md)。

**当前受限 G0 证据见本文件首节。** 仍需完成真实安全链和语义缺口；暂不把旧分析器重新拼装回来。以下路线继续有效；更早的修复记录均是删减前历史。

---

## 历史核心计划：2026-09-22 的 G0/G1/G2（已被上方受限路线取代）

**以下为旧版计划，不再作为当前执行顺序。** 不再把整体追平 Tai-e 作为近期验收目标。 建议固定分析器构建工具链、独立验证目标项目工具链，借鉴 Tai-e 的必要机制，先完成一条真实跨函数安全链。继续冻结旧 AST 能力扩张；Python 原型保留为语义对照。

本轮已重新核查官方基础设施、公开 compiler 版本并实测最小合法包。完整依据、组件取舍与停止条件见 [MoonBit 基础设施与 Tai-e 核心取舍复核](docs/moonbit-infrastructure-research-2026-09-22.md)，原始命令及输出见 [机器记录](docs/metrics/moonbit-infrastructure-2026-09-22.json)。

### 改变路线判断的新证据

- 当前工具链是 moonc v0.10.14；公开 compiler `main` 实测仍为 2025-09-29 的 `d4ada10d`。旧 IR dump 补丁未编译，也未证明可用于当前发行版，不能当作现成入口。
- 新版 `moon ide` 在合法小包中能返回类型、直接调用定义位置和局部引用，值得复用；但没有验证到完整、稳定、可批量消费的带类型 IR。`-dsource` 本次实测也未输出文本语义。
- 旧研究中“自建类型重建不可避免”“导出 Core 即获得完整派发精度”的判断撤回。先验证官方语义入口，接受固定工具链依赖，不优先自建完整类型系统。

### 从 Tai-e 借鉴的范围

1. **统一程序表示**：规范身份、显式调用绑定、源码位置及顺序 IR。
2. **数据流求解**：基本块工作队列、依赖通知、递归不动点和明确预算状态。
3. **对象/字段语义**：分配点对象、别名、覆盖/合并规则，返回和异常使用同一套堆状态。
4. **安全模型与证据**：规范 API 身份上的 source/sink/transfer/sanitizer，能解释的告警路径。

间接调用所需的指针/调用图反馈按实际需求加入；先不做多种上下文策略、通用插件平台、生产增量缓存或 Java/Android 特性适配。直接抽取 Tai-e PTA 不能自动解决顺序覆盖，仍需保留顺序敏感的数据流核心。

### 明确的下一步与停止条件

1. **G0，入口裁决，建议最多 3 个工作日**：验证当前发行版能交付哪些绑定、类型和顺序事实，落地前端契约与真实探针。官方批量接口优先；IDE 查询必须验证完整性与成本；compiler 导出必须先证明版本匹配。无法建立可靠契约时，不转向无限补齐自有类型系统，改做边界明确的有限分析产品。
2. **G1，一条安全链**：先以 CWE-113 的输入→helper→字段→返回/异常→header sink 为目标。别名先写后读、后写清理、不同对象、跨包、净化和递归必须从真实 `.mbt` 自动进入新核心。泛型 trait、默认参数、闭包、async/FFI 的未支持边界显式披露，不改写旧失败预期。
3. **G2，性能与迁移**：入口通过后再优化全堆快照成本。固定语料，公开支持范围、耗时、内存及不完整计数；建议初始每项目 60 秒/1 GiB 预算，含前端。完整覆盖已承诺的安全链及依赖，正确性与资源门槛均通过后，才迁移该范围。
4. **淘汰标准**：仍须旧 AST 预计算污点、第二套堆或例子专用语义分支才能过关，则淘汰候选实现。未支持项不能按安全结果计入完成率。

### 已有原型证据仍然有效，但不代表生产可用

[最小核心语义规格](experiments/core_semantics/README.md) 有 15 个手写 IR 场景、256 个生成程序和 14 项测试；支持别名、顺序覆盖、正常/异常出口与保守预算结果。128 次串联调用为 387 次传递。

**现已接入受限真实 MoonBit 前端，但全堆快照成本与真实语料性能未验收。** 不据此关闭原 CLI 的两个别名反例；此前 B1→B2→B3 的执行顺序改由本节 G0→G1→G2 替代。详情见 [核心语义链验证](docs/core-semantics-validation-2026-09-22.md)。

以下字段修复记录保留作历史对照；其中按旧 AST 路线继续补别名/派发的执行顺序已被本节替代。旧流水线的机械门禁不能作为新核心迁移的证据。

---

## 路线调整前的字段修复记录（2026-09-22）

当前对象仍是基于 `41f76dc` 的未提交工作区。已迁移到 **moonc v0.10.14、parser/moon_config/lexer 0.4.0、x 0.5.5**；修复新版编译器要求的显式测试导入和公开 trait 方法导出，并同步生成接口。工具链安装在 `/tmp` 的独立目录，本机默认旧版本没有被替换。

### 已验证的进展

- 原始 **8 个合法反例在 AST/CFG 两种扫描模式中全部符合预期**。内部 source 返回、标签/位置参数、函数参数遮蔽、点调用字段副作用、常量不可达均有真实 CLI 回归。该结论只覆盖已列出的语义边界。
- 默认注册表的 PointsTo、CallGraph、TaintFlow 传递同一联合状态；调用图投影不再重新执行旧指针求解。污点方法解析消费共享目标，并将其纳入缓存依赖。
- **7 项联合回归通过**：调用边反馈、堆对象身份、标签/默认实参、嵌套调用点区分、预算耗尽、注册表污点消费和不完整接收者的保守候选。
- 工厂方法用例经过默认注册表产生预期告警；清除共享目标后告警消失，证明测试覆盖了实际接线和热缓存失效。
- 新增异常退出事实，携带错误类型、payload 和抛错时状态；调用摘要、catch、普通 defer 与 errdefer 使用同一退出事实。合法异常项目已扩充至 **24 项，AST/CFG 正常扫描全部符合精确行号预期**。
- 跨包同名异常、异常摘要变化和正常返回能力变化的热缓存回归通过。单行/多行 import 统一使用官方配置 AST；显式别名不再额外注册默认别名。
- 生产解析入口统一为 `parse_source`：Handrolled 失败时尝试 MoonYacc；插值原始文本使用官方表达式解析器按原坐标展开。字节插值和字符串插值保留变量污点及内部调用，简单插值可独立执行 CFG。
- async 固定快照的 **170 个文件解析错误归零，冷/热扫描退出码均为 0，并通过新编译器检查**。解析策略版本进入缓存键，实测升级前三个已有函数缓存均失效，新策略热缓存稳定。
- 当前 CLI **61/61**、harness **33/33**、cases 通过；全目标检查/构建、格式与生成接口同步通过。冻结前最终源码的四后端完整测试各 437/437；这是旧流水线证据，不是新核心验收。
- cases 门禁发现并推动修复了“遮蔽检查误禁显式回调模型”的回归；当前 cases 和 harness 已通过。全量结果及工具链信息见[升级修复记录](docs/upgrade-review-2026-09-22.md)。

### 本轮新增的修复与复核结论

- UTF-8 模型按完整导入包身份匹配，只传播字符串实参，`bom` 不污染返回；别名和同名普通函数有安全对照。
- 泛型 `Show` 在摘要中保留待实例化关系，调用处按实际类型选择 `Show::to_string` 或默认 `output` 的写入内容；包含 Logger 别名、辅助写入、`write_object`、多层包装和方法包装。
- 泛型 sink 同样保留待实例化关系，避免在泛型定义处无条件告警；返回关系、写入内容和 sink 效果都进入 SCC 稳定签名与缓存指纹。
- **20 个转换项目全部可编译，AST/CFG 正常模式均满足精确行号预期**。另有实现从安全改为 source、再改回安全的热缓存测试，以及跨包同名类型测试。
- mocket 原两处净化后编码误报消失，当前固定快照为 **42 文件、0 告警**；crescent **53/5**、async **170/0**，均无解析错误，冷/热结果一致。零告警不表示完整安全验收。
- UTF-8 简单包装 strict 返回 0；泛型格式化和泛型 sink strict 返回 2，仍是整函数回退，不能记作独立 CFG 支持。

### 本轮默认参数修复

- 默认表达式在**调用时的被调函数词法环境**中执行，先绑定显式参数，再依次计算缺省参数；返回、sink、字段和异常消费者复用同一绑定，不再各自取预先算好的默认污点。
- 显式覆盖跳过默认表达式；转发 `Some` 跳过、`None` 执行，未知存在性合并两条路径。存在性随声明身份、赋值、分支、异常状态和循环稳定性检查传播。
- 默认表达式里的 sink 重定位到触发它的调用点；抛错时保留堆状态，抛错路径不再继续调用函数体。显式 source/sink 模型也消费默认绑定，不能跳过默认效果。
- **22 项合法默认参数项目全部通过 AST/普通 CFG**，覆盖链式依赖、方法、堆副作用、异常、求值顺序、模型、泛型 Show 和工厂返回。另有跨包同名 helper、跨包泛型类型、跨文件默认表达式“安全→source→sink 效果→安全”的冷/热缓存测试。
- 当前独立 CFG 还不执行默认表达式：相关调用整函数回退，strict 返回 2；全部显式覆盖的简单对照仍 strict 返回 0。旧默认值测试此前的 strict 通过不能当作该能力已实现。
- 规范类型事实随 let/赋值/分支传播，补上安全字符串别名和泛型默认值的实际类型。仍未替换所有旧分派类型路径，不能宣称全局类型身份已统一。
- 默认修复阶段新增 `taint-policy=call-defaults-and-type-facts-v1` 进入缓存指纹；真实旧/新策略二进制验证三个既有函数指纹均改变，新策略热缓存稳定。默认展开深度上限为 16，耗尽保守保留未知事实并披露计数，递归默认值已验证不会无声截断；耗尽计数非零时不能视为默认效果已完整分析。

### 本轮字段修复：原三类漏报关闭，扩展边界仍未全通过

- `FuncTaintSummary.field_taint` 从单个来源参数改为完整 `SecurityFact`，保存独立 source、多来源、字段来源和 clean 写入。静态调用与点调用都更新实际对象的指定字段；不再污染整个接收对象。
- 摘要入口按形参建立对象标识，let 别名和同名声明遮蔽保留原对象身份。记录构造使用与联合指针侧相同的 `alloc:文件:起始偏移` 格式，关闭同一行对象碰撞；**heap/sites 仍是独立状态，不能视为完整堆已共享**。
- 分别汇总正常返回、提前 return、抛错出口的字段值；未写路径保留原字段来源。调用时所有字段右值先按入口堆实例化，正常写入不再泄漏到 catch。getter、复制和交换按字段来源投影，已知安全字段不再混入整个对象的污点。
- 正常和异常字段事实都进入 SCC 稳定签名及缓存指纹，冻结前最终策略为 `taint-policy=bounded-field-exit-facts-v2`。跨包同名 Box/get/fill，以及“全安全→仅正常出口脏→仅异常出口脏→全安全”的冷/热缓存验证通过。
- [field_probe.py](docs/review-fixtures/2026-09-22/field_probe.py) 的 **23 个项目均可编译，21 项 AST/普通 CFG 精确行号通过，2 项仍失败**。已通过项覆盖字段清理、无关字段、多层别名、同名遮蔽、条件写入、提前返回、异常出口、defer、同一行分配、getter、复制和交换；相关 strict 均返回 2，尚未证明独立 CFG 字段执行。
- CLI **61/61** 包含已关闭的 21 项字段语义和跨包缓存门禁。两个未关闭项保留原始正确预期，不进入“已关闭项”通过统计；完整字段探针仍退出 1，不能把 CLI 全绿解释为字段验收完成。

### 七项反例复核：六项关闭，一项仍失败

[remaining_probe.py](docs/review-fixtures/2026-09-22/remaining_probe.py) 七个项目均通过新编译器。当前 AST/普通 CFG 的结果如下；完整脚本仍退出 1。

| 状态 / 反例 | 预期行 → 实际行 | 当前结论 |
| --- | --- | --- |
| 已关闭 `default_parameter_dependency` | `[5]` → `[5]` | 缺省值按实参顺序求值；独立 CFG 执行仍待实现。 |
| 已关闭 `default_sink_effect` | `[5]` → `[5]` | 缺省调用保留 sink 效果，显式覆盖跳过；默认专项 22 项仍通过。 |
| 已关闭 `field_internal_source` | `[8]` → `[8]` | 完整字段事实保留函数内部 source。 |
| 已关闭 `field_alias_parameter` | `[8]` → `[8]` | 形参对象身份沿别名传播，写入可更新调用方对象。 |
| 已关闭 `field_multiple_origins` | `[8]` → `[8]` | 多来源逐项实例化；两个安全字符串不会因 flag 参数脏而告警。 |
| P1 `generic_trait_source` | `[7]` → `[]` | 普通 trait 仍缺延迟派发；不能用实参污点并集替代实现返回语义。 |
| 已关闭 `show_local_literal_alias` | `[]` → `[]` | 规范类型事实随局部绑定传播，转换专项 20 项仍通过。 |

### 本轮复核意见：下一步必须处理形参之间的别名

完整字段事实解决了来源丢失，但**分别计算形参的最终字段值仍不够**。当两个形参实际指向同一对象，函数内部的写入会改变另一个形参后续读取的值；在调用处只合并最终值，无法恢复这条依赖。

| 优先级 / 新反例 | 预期行 → 实际行 | 根因 |
| --- | --- | --- |
| P1 `two_formals_alias_read_after_write` | `[8, 9]` → `[8]` | `first.value = source(); second.safe = second.value`，调用 `fill(box, box)` 后第二字段漏报。实例化错误地读取入口旧值，单纯把冲突写入做 join 无法保证不漏报。 |
| P1 `two_formals_alias_clean_last` | `[]` → `[8]` | `first.value = source(); second.value = "safe"`，同一对象的后写清理被最终值合并丢掉，产生误报。 |

明确执行顺序：

1. 在调用绑定处按实际对象集合计算形参之间的必然/可能别名，接收者与标签参数使用同一绑定。**不要通过内容污点来源推断对象别名。**
2. 优先引入带顺序的字段读写效果，或按别名分组重新实例化函数摘要。先写后读必须看见同一对象的新值；必然同一对象的后写应覆盖前写，可能别名则保留两种路径。禁止依赖 Map 遍历顺序选择最后写入。
3. 保留正常/异常出口分离，覆盖清理、提前返回、多层包装和递归。若按别名分组缓存，分组、被调方效果及预算策略都必须参与缓存键；预算耗尽应显式披露并保留未知效果。
4. 将上述两项转绿，再增加相反写入顺序、两个不同对象的安全对照，以及修改字段读写顺序后的跨文件冷/热缓存测试。**字段完整探针退出 0 才能关闭这条语义链。**
5. 随后处理普通泛型 trait。完整共享堆、全局身份、CallSite(1) 和独立 CFG 字段/默认/异常执行仍按原验收要求继续，不能因这 21 项通过提前关闭。

### 新发现的阻塞项与执行顺序

| 优先级 | 当前状态/问题 | 下一步改动 | 验收条件 |
| --- | --- | --- | --- |
| P1 | 原始 `errdefer` 三个错误已关闭；扩展 24 项通过，独立 CFG 仍回退 | 继续验证循环 else、break/continue 的退出状态和 cleanup，以及外部函数错误效果；独立 CFG 加入异常边 | 保留 24 项正反例、跨包身份及两种摘要热缓存失效；strict 对未支持场景仍返回 2，不计为独立 CFG 完成 |
| P1 | UTF-8 与 core Show 的 20 项转换反例已通过；局部别名已修，普通泛型 trait 仍失败 | 继续传播规范类型事实并推广延迟 trait 派发，保留现有转换安全对照 | conversion_probe 20 项不退化，remaining_probe 的普通泛型项转绿，局部别名不退化，跨包和热缓存仍正确 |
| P2 | 真实快照编译验收已执行：async 通过；mocket 全模块有旧 strconv API；crescent 有旧 lexscan 输入类型错误 | 保留原快照和依赖哈希，明确有效扫描范围；为后续语义验收固定可编译的新版本或单独记录迁移差异 | mocket 扫描库路径已通过；其他编译失败不得解释为分析器安全结论 |
| P1 | 字段完整事实与构造 ID 格式已接入；多形参别名仍有一漏报、一误报，污点堆仍独立 | 保留字段读写顺序或按调用处别名分组实例化，再接完整共享堆 | 字段 23 项全部转绿，新增写入顺序变化的冷/热缓存测试；保留工厂对象和注册表断线验证 |
| P1 | 全局函数身份尚有旧类型推断回退和传递 sink 路径 | 统一绑定、SCC、返回/字段/传递 sink；消除源内 trait 目标的不同字符串身份 | 跨包、不同 trait 同名方法、重命名及文件顺序不变性同时通过目标与告警断言 |
| P1 | 默认参数 22 项与 strict 披露已修；独立 CFG 尚不执行默认表达式 | 在 CFG 中显式降低默认求值、条件效果和绑定顺序，保持当前 AST 行为 | 正常扫描不退化；实现后才将对应 strict 从 2 改为 0，并证明独立效果来自 CFG |
| P2 | 新 parser 能解析更多语法，不等于分析语义已支持 | 为新增语法建立可编译语料，明确独立 CFG 支持与整函数回退范围 | strict 模式对未支持语义继续失败；正常模式仍须满足预期告警 |
| P2 | CallSite(1) 仍明确未实现；固定真实语料尚未完成新验收 | 实现真正带上下文身份的节点/摘要，再按固定版本语料和缓存状态验收 | 分离上下文改变实际结果，且通过递归、预算耗尽与缓存失效测试 |

### 为什么不能宣布完成

旧的“7 个原始场景失败”结论已经过时，保留在[历史复核](docs/review-continued-2026-09-22.md)用于对照。本轮已关闭新版本异常语义的已列反例，但循环退出等相邻边界仍需系统覆盖，且“共享调用目标”不等于“共享完整堆与上下文事实”。后续应继续按完整调用链和最终告警验收，不能以接口接通或已有测试全绿代替要求完成。

下一轮先关闭多形参别名的读写依赖漏报和清理误报，再完成普通泛型 trait、共享堆及统一身份；独立 CFG 默认求值仍需实现。异常控制流新增用例进入持续门禁；循环执行零次路径已补回，但不据此宣称循环/异常语义全面完成。每项保留合法正反例、默认入口和断线验证，完整验收前不标记整体完成。

---

## 历史分析与整体路线（2026-09-16）

整理日期：2026-09-16。

本文整理前一轮关于“为什么一直没修完善成功”的分析和执行建议。事实依据是对提交 `5b2d867` 的[差距复核](docs/review-taie-gap-2026-09-16.md)与[验证记录](docs/metrics/taie-review-2026-09-16.json)。工作区后续修复需要重新验收，本文不把旧结论直接当作最新代码的状态。

## 一、为什么修复总是没有闭环

主要原因是：**实现与验收经常停在局部结构上，没有证明真实入口使用了这些结构，也没有证明结果符合预期语义。** 单个反例修复后，底层身份、事实传播和入口接线仍不统一，于是相邻场景继续出现同类问题。

| 原因 | 已验证的例子 | 应改变的做法 |
| --- | --- | --- |
| 局部补丁没有覆盖共同根因 | 加入 `Type::method` 修好了类型同名反例，跨包同名仍串用摘要 | 从定义、调用绑定到摘要消费统一函数身份 |
| 测试断言不能区分正确与错误 | 告警总数都是 2，但可能是正确两处，也可能包含一处漏报和一处误报 | 同时断言必须出现和必须消失的具体告警 |
| 直接 API 正确，生产适配层断线 | 直接调用图 API 有事件，经默认注册表为 0；库入口与 CLI 的符号加载选项不同 | 经过真实 CLI 和默认注册表验收 |
| 新机制仍依赖旧机制的结果 | CFG 执行前由 AST 生成事实，预计算表遮蔽按当前块状态求值 | 用接口约束建立可独立执行的最小语义链 |
| 配置与能力声明先于求解能力 | CallSite(1) 标记为已实现，普通调用却没有形成上下文摘要 | 检查实际节点身份、传播事实与可区分结果 |
| 指标与名称含义不一致 | 指向集合非空计为目标集合缩小；缓存命中使执行诊断下降 | 明确定义每个指标的分母、触发条件和缓存口径 |
| 一轮同时推进过多基础能力 | 身份、CFG、上下文、指针桥接、事件、缓存交叉变化 | 每轮只关闭一个语义链，完成后再扩大范围 |

这不意味着现有工作没有价值。共享依赖结果接口和原类型同名反例都有实际进展。问题在于，局部进展被过早解释成了整个能力完成。

## 二、先改变修复与验收方式

### 1. 每项修复先提交一个会失败的语义测试

测试至少说明以下内容：

- 输入程序应能通过编译，避免把无效程序行为当作分析缺陷。
- 必须报告哪个 sink，必须不报告哪个 sink。
- 测试经过哪个入口：CLI、默认注册表或底层求解器。
- 当前版本为什么失败，修复后预期发生什么变化。

对接线类修复做一次临时断线验证：撤掉新增的结果传递或事件转发后，对应测试必须失败。无需引入完整变异测试框架，只需确认测试确实覆盖了这条连接。

不要用以下断言证明功能完成：

- 集合长度大于等于 0。
- 只检查不崩溃、目录存在或 `implemented=true`。
- 只检查告警总数，不检查告警位置与类型。
- 用直接 API 测试代替生产入口测试。

### 2. 将完成状态拆成四级

| 状态 | 所需证据 |
| --- | --- |
| 接口已存在 | 数据结构和调用接口可用 |
| 生产路径已接入 | CLI、默认注册表和实际消费者确实使用它 |
| 语义已验证 | 正反例、边界条件和不变性测试通过 |
| 真实语料已验证 | 固定版本语料有可复现结果，关键发现经过核对 |

提交说明和能力文档应写清达到哪一级。对尚未支持的上下文策略或语义，应明确报告支持范围，避免仅修改测试预期来消除失败。

### 3. 每轮保留一份可复核的验收记录

记录代码版本、工具链、语料版本、运行命令、缓存状态、预期与实际结果。区分告警精度、解析覆盖、调用绑定覆盖和执行耗时，不用一个汇总数字替代其他指标。

## 三、下一轮只完成全局函数身份

**下一轮目标：让 ProgramWorld、调用绑定、SCC、摘要生成和摘要消费使用同一套函数身份，关闭跨包/trait 同名污染。**

### 改动顺序

1. 在 `ProgramWorld` 中建立规范的 `PackageId`，由模块与包信息确定归属。
2. 建立结构化的 `FunctionId`：至少区分所属包、普通函数/固有方法/trait 实现，以及所属类型和 trait；具体字段以 MoonBit 的合法定义规则为准。
3. 建立 `CallSiteId`：包含调用者身份和表达式位置或节点编号，能区分同一行中的两个调用。
4. 在绑定阶段解析本地调用、包别名调用、方法调用和 trait 调用；静态调用得到目标 `FunctionId`，动态调用保留候选集与解析状态。
5. SCC 从绑定后的调用关系建边，摘要表用 `FunctionId` 存取，污点传播消费同一绑定结果。
6. 检查调用图、指针节点和缓存键中的函数身份，移除会重新合并不同定义的短名称键。

展示给人的名称可以单独保留，分析内部身份不能依赖展示字符串的拼接或后缀猜测。

### 应避免的补丁

- 从第一份源码文件所在目录推断整个项目根目录。
- 把导入函数一律加上调用者包前缀。
- 在摘要表中搜索唯一名称后缀，并将“暂时只有一个候选”当作绑定成功。
- SCC 用一种名字，摘要生成用另一种名字，消费时再尝试兼容。

这些做法会让新增无关包、改变文件遍历顺序或引入同名符号影响已有程序的分析结果。未解析调用应显式记录并按既定保守策略处理，不能静默借用另一个函数的摘要。

### 必须通过的验收

| 用例 | 预期 |
| --- | --- |
| 不同类型的同名方法 | 各自命中正确摘要，保留已有修复 |
| 不同包的同名函数 | 脏数据返回路径报告，安全返回路径无告警 |
| 合法的不同 trait 同名方法 | 按实际实现区分身份与目标 |
| 包导入与别名调用 | 绑定到被导入包中的定义 |
| 文件遍历或声明顺序变化 | 规范化后的语义结果一致 |
| 无关符号重命名或新增无关包 | 原有调用绑定与告警不变 |

测试应同时检查绑定目标和最终告警。重排导致源码行号变化时，按具名函数/sink 标记比较语义结果；对固定反例仍应精确断言告警位置。增加递归及互递归用例，确认 SCC 改造后仍可收敛。

**完成条件：**上述测试通过，真实 CLI 能复现修复效果，已有相关回归通过，正常解析路径不再依赖短名称或后缀查找摘要。

## 四、随后建立真正独立的最小 CFG 执行链

先限定受支持的子语言：常量、变量复制与赋值、条件分支、普通调用、返回、source/sink。闭包、异常、defer 和复杂堆语义分别扩展验收。

### 具体实现要求

- IR 必须携带可执行的语义，例如 `Const`、`Copy`、带实参与返回目标的 `Call`、`Branch`、`Return`；需要表达式操作时补充对应指令。
- CFG 执行器只接收 IR、模型、被调函数摘要及分析配置，不接收 AST walker 生成的污点结果表。
- 表达式使用当前块状态求值，分支状态通过显式合并规则汇合，循环按工作队列迭代到稳定状态。
- AST 可作为对照实现，不得成为新执行器语义正确性的隐含输入。
- 遇到尚不支持的语义时，明确按整个函数回退，并记录原因；避免在一次分析中混用两个执行器的中间事实。

### 验收用例

1. 常量赋给局部变量再传入 sink：无告警。
2. source 赋给局部变量再传入 sink：有告警。
3. 污点变量被安全常量覆盖：按覆盖后的状态判断。
4. 只有一个分支含污点：合并后保留可能的污点。
5. 普通调用返回污点：通过正确摘要传播。
6. 循环需要多轮传播：结果稳定，不依赖遍历顺序。

**完成条件：**受支持子集无需预先执行 AST 污点分析即可独立输出正确告警；相应反例的冷扫描与 `--cfg-verify` 均通过。不支持的函数具有可见回退原因。

## 五、统一指针与调用图后，再做真正的上下文敏感

建议依赖顺序：

```text
统一身份与调用绑定
  → 可独立执行的基础 IR
  → 上下文不敏感的指针/调用图联合求解
  → 污点分析消费统一事实
  → 真正的上下文敏感
  → 插件扩展与性能优化
```

### 先完成上下文不敏感的联合求解

打通以下循环，直到没有新增事实：

1. 新指向事实触发动态调用目标解析。
2. 新调用边加入实参到形参、返回值到调用结果的传播约束。
3. 新约束产生更多指向事实，重新触发相关调用点。

堆对象与字段传播必须保留统一的抽象身份，避免跨过程传递时降成仅按类型归并的占位字符串。污点分析可保留现有流敏感能力，但应消费同一调用目标和堆事实。

默认注册表需要明确依赖关系并传递结果。事件应覆盖其名称承诺的新增事实；接入后，以真实入口测试验证回调和反馈均可继续驱动求解。

### 再验收 CallSite(1)

上下文必须进入实际的方法实例、局部变量和所选堆抽象的身份与传播过程。堆上下文策略应单独说明，不能只给摘要键增加字符串。

验收需要一个能区分上下文策略的程序：同一个分配点经两个不同调用点产生对象，一条路径写入污点，另一条保持安全，检查对象与字段事实是否按设计隔离。同时检查上下文节点、调用目标和最终告警。

`helper(x) { x }` 分别接收安全参数和污点参数并不足以证明上下文敏感：普通参数到返回值的关系摘要也可以得到正确结果。同一行的两个调用也必须具有不同 `CallSiteId`。

## 六、缓存、指标与入口的收尾标准

这些问题需要各自的回归测试，可作为独立修复提交；它们不应被用作核心分析能力已经完成的证明。

| 项目 | 最小验收 |
| --- | --- |
| CLI 与库入口一致性 | 必需的符号信息、配置和分析依赖一致；真实 CLI 能触发新增能力 |
| 注册表事件 | 在应有事件的程序上精确断言事件内容；断开转发后测试失败 |
| 指针精度指标 | 分开统计有指向信息、严格缩小、等集、空交集回退；人工反例有明确预期 |
| 缓存生命周期 | 独立进程首次写入、再次命中、源码变化后更新、依赖变化后失效 |
| 缓存诊断 | 分开显示已执行、缓存复用和未支持数量；缓存命中不被误解为分歧消失 |
| 真实语料 | 固定代码版本，区分解析失败与零告警，人工核对关键发现 |
| 性能 | 记录 release 构建、冷/热缓存、相同语料与配置；先保证结果可比 |

## 七、下一项任务可以直接这样下达

> 完成全局函数身份闭环。先把已复现的跨包同名漏报/误报写成真实 CLI 回归测试，精确断言安全与危险 sink；再由 ProgramWorld 提供包身份，在绑定阶段产生统一 FunctionId，让 SCC、摘要生成和摘要消费共用它。补齐类型/trait 同名、导入别名、顺序变化、无关符号重命名与递归测试。本轮范围限于身份链路及其必要适配。交付修复前后的测试证据、受影响模块说明和剩余限制。

后续每一轮都采用同样的完成标准：**一个明确的语义问题，一条真实入口链路，一组能证明修复有效的验收测试。**


## 2026-09-25 原生交付与首条真实链复核

按用户确认的方向，生产交付采用纯原生 CLI，首批覆盖 Linux/macOS/Windows。已实现原生工具链检查、真实后端文件计划、项目快照和统一报告，Linux 归档完成搬迁验收；另外两平台仍需真实 runner 执行，不应写作全平台完成。

subagent 发现并推动修复：父级验证选项被 baseline 子命令丢弃、FIFO 源码导致同步读取挂起、快照跳过目录与扫描集合不一致、默认文本展开全部快照。新增反例进入独立交付验收，不通过时不放行。

mocket 0.9.1 固定快照的真实 HTTP dispatch 对照 8/8 通过，必需声明绑定唯一。关键架构约束已经有执行证据：responder构造不等于路由输出；HTML文本编码不能清除脚本上下文风险；hover显示String不能区分内建与用户自定义类型。新核心必须遵守这些事实，不能退回名称匹配或为成功样例增加传播特判。当时生产语义模式尚未放行；该历史状态已被本文当前裁决替代。


</details>
